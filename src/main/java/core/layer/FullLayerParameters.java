package core.layer;

import core.ActFunc;

public class FullLayerParameters extends LayerParameters {
	public FullLayerParameters(int outputLength, ActFunc actFunc) {
		super(outputLength, actFunc);
	}
}
